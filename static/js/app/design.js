/*
new Vue({
            el: '#app',

            data: {
              elem: document.querySelectorAll('.sidenav'),
              instance: M.Sidenav.getInstance(elem),
            },

            created: function() {

            },

            methods: {
                opensidenav: function() {
                  var elem = this.elem
                  instance.sidenav("open");

                },
                closesidenav: function() {
                  var elem = this.elem
                  instance.sidenav("close")();

                },
            }
        });
*/
